#!/bin/bash
# Apply Tailscale Funnel configuration

CONFIG_FILE="$HOME/.config/tailscale/funnel-config.sh"

if [ ! -f "$CONFIG_FILE" ]; then
    echo "Error: Config file not found at $CONFIG_FILE"
    exit 1
fi

chmod +x "$CONFIG_FILE"
echo "Running master funnel config..."
bash "$CONFIG_FILE"
